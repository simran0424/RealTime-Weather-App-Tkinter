# 🌦️ Weather GUI Application (Python)

A simple Weather Application built using Python Tkinter that fetches real-time weather data using the OpenWeatherMap API.

## 🔧 Technologies Used
- Python
- Tkinter (GUI)
- Requests Library
- OpenWeatherMap API

## 🚀 Features
- Enter any city name to get:
  - Temperature
  - Atmospheric pressure
  - Humidity
  - Weather description
- User-friendly GUI
- Error handling for invalid city names

## 🛠️ How It Works
1. User enters a city name.
2. Application sends a request to OpenWeatherMap API.
3. Weather details are fetched and displayed in the GUI.

## ▶️ How to Run
1. Install required library:
   ```bash
   pip install requests
Run the application:
python weatherapp.py

🔑 API Key

This project uses OpenWeatherMap API
Replace the API key in the code with your own if required.

📌 Example Cities

Mumbai, Delhi, London, New York, Tokyo

📷 Output

Displays live weather details in a Tkinter window.